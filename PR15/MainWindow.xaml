<Window x:Class="PR15.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PR15"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"  Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Cборка ПК" Click="BtnBuilder_Click" Padding="5" Background="Orange" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,10,0"/>
                <Button Content="Сохраненые сборки" Click="BntSaved_Click" Padding="5" Background="Orange" Foreground="White" FontSize="14" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
