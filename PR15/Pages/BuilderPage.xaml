<Page x:Class="PR15.Pages.BuilderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PR15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BuilderPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Orange" FontWeight="Bold"/>
                    <TextBox x:Name="TbxSearch" Width="200" TextChanged="TbxSearch_TextChanged" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                    <TextBlock Text="Производитель:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Orange" FontWeight="Bold"/>
                    <ComboBox x:Name="CmbManufacturer" Width="150" SelectionChanged="CmbManufacturer_SelectionChanged"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                    <TextBlock Text="Тип детали:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Orange" FontWeight="Bold"/>
                    <ComboBox x:Name="CbCategory" Width="200"  SelectionChanged="CbCategory_SelectionChanged"/>
                </StackPanel>
            </WrapPanel>

            <ListView x:Name="LvCatalog" 
                  Grid.Row="1" 
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDDDDD" BorderThickness="1" Background="White" Padding="10" Margin="0,2" CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding image}" Width="64" Height="64" Margin="0,0,15,0" Stretch="Uniform"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding FullDescription}" Foreground="Gray" FontSize="12" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding manufacturer_.name}" Foreground="Gray"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding price, StringFormat={}{0:N0} ₽}" FontWeight="Bold" FontSize="16" Foreground="Orange" HorizontalAlignment="Right"/>
                                    <Button Content="Добавить" Click="BtnAdd_Click" Padding="10,5" Margin="0,5,0,0" Background="White" Foreground="Black" BorderThickness="1" FontWeight="Bold" BorderBrush="Gray"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Border Grid.Column="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1,0,0,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Ваша сборка" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <ListView x:Name="LvCart" 
                      Grid.Row="1" 
                      BorderThickness="0" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding name}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Button Grid.Column="1" Content="❌" Click="BtnRemove_Click" Background="Transparent" BorderThickness="0" Foreground="Red" FontSize="14"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="2" Margin="0,15,0,0">
                    <Separator Margin="0,0,0,10"/>
                    <TextBlock Text="Итого:" Foreground="Gray"/>
                    <TextBlock x:Name="TbTotalPrice" Text="0 ₽" FontSize="22" FontWeight="Bold" Foreground="Orange" Margin="0,0,0,15"/>
                    <TextBlock Text="Название сборки:" Foreground="Gray" Margin="0,5"/>
                    <TextBox x:Name="TbAssemblyName" Width="200" Margin="0,0,0,5"/>

                    <TextBlock Text="Ваше имя (автор):" Foreground="Gray" Margin="0,5"/>
                    <TextBox x:Name="TbAuthorName" Width="200" Margin="0,0,0,10"/>
                    <Button Content="Сохранить сборку" Padding="10" Background="Orange" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnSaveAssembly_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
